<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.globalgates.mapper.PostMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into tbl_post (member_id, post_status, title, content, location, reply_post_id)
        values (#{memberId}, #{postStatus}, #{postTitle}, #{postContent}, #{location}, #{replyPostId})
    </insert>

    <update id="update">
        update tbl_post
        set title = #{postTitle},
            content = #{postContent},
            location = #{location},
            updated_datetime = now()
        where id = #{id}
    </update>

    <update id="delete">
        update tbl_post
        set post_status = 'inactive',
            updated_datetime = now()
        where id = #{id}
    </update>

    <!-- 게시글 단건 조회 -->
    <select id="selectById">
        select p.id,
               p.member_id,
               p.post_status,
               p.title          as post_title,
               p.content        as post_content,
               p.location,
               p.reply_post_id,
               p.created_datetime,
               p.updated_datetime,
               m.member_nickname,
               m.member_handle,
               (select f.file_name from tbl_member_profile_file mpf
                   join tbl_file f on mpf.id = f.id
                where mpf.member_id = p.member_id and mpf.profile_image_type = 'profile'
                limit 1) as member_profile_file_name,
               (select count(*) from tbl_post_like pl where pl.post_id = p.id) as like_count,
               (select count(*) from tbl_post rp where rp.reply_post_id = p.id) as reply_count
        from tbl_post p
                 join tbl_member m on p.member_id = m.id
        where p.id = #{id}
          and p.post_status = 'active'
          and p.reply_post_id is null
    </select>

    <!-- 게시글 목록 조회 (메인 피드) -->
    <select id="selectAll">
        select p.id,
               p.member_id,
               p.post_status,
               p.title          as post_title,
               p.content        as post_content,
               p.location,
               p.reply_post_id,
               p.created_datetime,
               p.updated_datetime,
               m.member_nickname,
               m.member_handle,
               (select f.file_name from tbl_member_profile_file mpf
                   join tbl_file f on mpf.id = f.id
                where mpf.member_id = p.member_id and mpf.profile_image_type = 'profile'
                limit 1) as member_profile_file_name,
               (select count(*) from tbl_post_like pl where pl.post_id = p.id) as like_count,
               (select count(*) from tbl_post rp where rp.reply_post_id = p.id) as reply_count
        from tbl_post p
                 join tbl_member m on p.member_id = m.id
        where p.post_status = 'active'
          and p.reply_post_id is null
        order by p.created_datetime desc
        limit #{count}
        offset #{offset}
    </select>

    <!-- 게시글 전체 개수 -->
    <select id="selectTotal" resultType="int">
        select count(*)
        from tbl_post
        where post_status = 'active'
          and reply_post_id is null
    </select>

</mapper>