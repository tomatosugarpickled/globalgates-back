<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.globalgates.mapper.PostLikeMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into tbl_post_like (member_id, post_id)
        values (#{memberId}, #{postId})
    </insert>

    <delete id="deleteByMemberIdAndPostId">
        delete
        from tbl_post_like
        where member_id = #{memberId}
          and post_id = #{postId}
    </delete>

    <select id="selectByMemberIdAndPostId">
        select id, member_id, post_id, created_datetime
        from tbl_post_like
        where member_id = #{memberId}
          and post_id = #{postId}
    </select>

    <select id="selectCountByPostId" resultType="int">
        select count(*)
        from tbl_post_like
        where post_id = #{postId}
    </select>

</mapper>
