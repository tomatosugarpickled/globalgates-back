<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.globalgates.mapper.ReportMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into tbl_report (reporter_id, target_id, target_type, reason, status)
        values (#{reporterId}, #{targetId}, #{targetType}, #{reason}, #{status})
    </insert>
</mapper>
