<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.globalgates.mapper.CategoryMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into tbl_category (category_name)
        values (#{categoryName})
    </insert>

    <select id="selectById">
        select id, category_name, created_datetime
        from tbl_category
        where id = #{id}
    </select>

    <select id="selectByCategoryName">
        select id, category_name, created_datetime
        from tbl_category
        where category_name = #{categoryName}
    </select>

    <select id="selectAll">
        select id, product_category_parent_id, category_name, created_datetime
        from tbl_category
        order by id
    </select>

    <delete id="delete">
        delete from tbl_category
        where id = #{id}
    </delete>
</mapper>